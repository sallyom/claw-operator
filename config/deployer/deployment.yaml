apiVersion: apps/v1
kind: Deployment
metadata:
  name: openclaw-deployer
  labels:
    app.kubernetes.io/name: openclaw-deployer
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: openclaw-deployer
  template:
    metadata:
      labels:
        app.kubernetes.io/name: openclaw-deployer
    spec:
      serviceAccountName: openclaw-deployer
      containers:
        - name: app
          image: openclaw-deployer:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
        - name: oauth-proxy
          image: quay.io/openshift/origin-oauth-proxy:4.21
          imagePullPolicy: IfNotPresent
          args:
            - --provider=openshift
            - --openshift-service-account=openclaw-deployer
            - --upstream=http://127.0.0.1:8080
            - --http-address=0.0.0.0:4180
            - --https-address=
            - --email-domain=*
            - --cookie-secret-file=/etc/proxy/secrets/session_secret
            - --pass-user-headers=true
            - --skip-auth-regex=^/healthz$
          ports:
            - name: oauth
              containerPort: 4180
          volumeMounts:
            - name: cookie-secret
              mountPath: /etc/proxy/secrets
              readOnly: true
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      volumes:
        - name: cookie-secret
          secret:
            secretName: openclaw-deployer-cookie
